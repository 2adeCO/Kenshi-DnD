# Here you can modify the database path, use the following format, replacing the values in {} with your own:
# Server={server};Database={database};user={user};port={port};password={password}
# Empty or invalid settings will change the application's mode to XML
server=localhost;user=root;database=kenshi_dnd_db;port=3306;password=root